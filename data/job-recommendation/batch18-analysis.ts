import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 18 (1701-1800위) 데이터 추가
const BATCH_18 = [
    '엔젤릭버스터', '나이트워커', '비숍', '소울마스터', '나이트로드',
    '아델', '나이트워커', '메르세데스', '미하일', '호영',
    '아크메이지(불,독)', '윈드브레이커', '나이트로드', '나이트로드', '아란',
    '블래스터', '나이트로드', '데몬어벤져', '섀도어', '아란',
    '나이트워커', '아크', '데몬어벤져', '보우마스터', '에반',
    '신궁', '캡틴', '듀얼블레이드', '나이트로드', '나이트로드',
    '캐논슈터', '아크메이지(썬,콜)', '아델', '비숍', '아델',
    '섀도어', '비숍', '에반', '나이트로드', '엔젤릭버스터',
    '나이트로드', '나이트로드', '아델', '다크나이트', '카데나',
    '아델', '카인', '섀도어', '듀얼블레이드', '제로',
    '비숍', '듀얼블레이드', '캐논슈터', '호영', '아란',
    '캐논슈터', '섀도어', '와일드헌터', '섀도어', '아델',
    '캐논슈터', '에반', '캐논슈터', '나이트로드', '아델',
    '섀도어', '비숍', '패스파인더', '나이트워커', '아크메이지(불,독)',
    '비숍', '호영', '은월', '아크메이지(불,독)', '아크',
    '듀얼블레이드', '제로', '듀얼블레이드', '팔라딘', '팔라딘',
    '아델', '섀도어', '듀얼블레이드', '라라', '패스파인더',
    '카인', '비숍', '아란', '렌', '아델',
    '데몬어벤져', '메르세데스', '비숍', '메르세데스', '나이트로드',
    '캐논슈터', '섀도어', '은월', '칼리', '히어로', '메르세데스'
];

// 배치 18 추가
addBatch(18, BATCH_18);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('🚀 배치 18 (1701-1800위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1701-1800위 구간 분석
const batch18Jobs = BATCH_18.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch18Top = Object.entries(batch18Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1701-1800위 구간 TOP 15:');
batch18Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1800위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 30 (1-1800위)】');
distribution.slice(0, 30).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎉 90% 완료! (1800/2000)');
console.log('💪 마지막 200개! 거의 끝났습니다!');
console.log('='.repeat(80));
