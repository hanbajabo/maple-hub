import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 13 (1201-1300위) 데이터 추가
const BATCH_13 = [
    '팔라딘', '배틀메이지', '비숍', '에반', '윈드브레이커',
    '팬텀', '아델', '다크나이트', '아델', '나이트로드',
    '메르세데스', '데몬어벤져', '윈드브레이커', '엔젤릭버스터', '소울마스터',
    '나이트워커', '히어로', '배틀메이지', '아델', '비숍',
    '팔라딘', '비숍', '듀얼블레이드', '아크메이지(썬,콜)', '제논',
    '바이퍼', '아크메이지(썬,콜)', '제로', '와일드헌터', '와일드헌터',
    '나이트로드', '아크메이지(썬,콜)', '나이트로드', '듀얼블레이드', '아크메이지(썬,콜)',
    '플레임위자드', '캡틴', '아델', '메르세데스', '제로',
    '에반', '캐논슈터', '듀얼블레이드', '아크메이지(불,독)', '듀얼블레이드',
    '아델', '메르세데스', '아크', '나이트로드', '히어로',
    '아크메이지(불,독)', '아델', '나이트로드', '패스파인더', '팔라딘',
    '듀얼블레이드', '엔젤릭버스터', '메르세데스', '듀얼블레이드', '팔라딘',
    '나이트로드', '아크메이지(썬,콜)', '나이트워커', '아란', '듀얼블레이드',
    '윈드브레이커', '아크메이지(불,독)', '비숍', '아크메이지(불,독)', '소울마스터',
    '엔젤릭버스터', '일리움', '신궁', '렌', '은월',
    '루미너스', '엔젤릭버스터', '은월', '아크메이지(불,독)', '윈드브레이커',
    '듀얼블레이드', '나이트로드', '다크나이트', '섀도어', '호영',
    '제논', '윈드브레이커', '듀얼블레이드', '제논', '듀얼블레이드',
    '호영', '팔라딘', '아란', '아크', '비숍',
    '제논', '아크메이지(썬,콜)', '은월', '히어로', '나이트로드'
];

// 배치 13 추가
addBatch(13, BATCH_13);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('📊 배치 13 (1201-1300위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1201-1300위 구간 분석  
const batch13Jobs = BATCH_13.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch13Top = Object.entries(batch13Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1201-1300위 구간 TOP 15:');
batch13Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1300위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 20 (1-1300위)】');
distribution.slice(0, 20).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('📈 65% 완료! (1300/2000)');
console.log('⏳ 남은 700개 (1301-2000위) 계속 진행하시겠어요?');
console.log('='.repeat(80));
