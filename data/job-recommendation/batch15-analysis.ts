import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 15 (1401-1500위) 데이터 추가
const BATCH_15 = [
    '보우마스터', '나이트로드', '배틀메이지', '듀얼블레이드', '아델',
    '카이저', '스트라이커', '호영', '비숍', '나이트로드',
    '제논', '아크메이지(불,독)', '듀얼블레이드', '비숍', '스트라이커',
    '소울마스터', '키네시스', '비숍', '아델', '은월',
    '다크나이트', '카이저', '윈드브레이커', '아델', '카이저',
    '아델', '라라', '카데나', '아델', '렌',
    '나이트로드', '나이트로드', '팔라딘', '에반', '패스파인더',
    '플레임위자드', '듀얼블레이드', '패스파인더', '비숍', '바이퍼',
    '칼리', '와일드헌터', '팔라딘', '듀얼블레이드', '제로',
    '비숍', '히어로', '은월', '제논', '아크메이지(불,독)',
    '아크메이지(불,독)', '히어로', '패스파인더', '엔젤릭버스터', '나이트로드',
    '아크메이지(불,독)', '나이트로드', '듀얼블레이드', '비숍', '듀얼블레이드',
    '비숍', '제로', '라라', '패스파인더', '듀얼블레이드',
    '팔라딘', '렌', '바이퍼', '은월', '나이트로드',
    '섀도어', '듀얼블레이드', '비숍', '듀얼블레이드', '제로',
    '비숍', '데몬어벤져', '에반', '비숍', '제로',
    '윈드브레이커', '다크나이트', '아크메이지(불,독)', '라라', '히어로',
    '듀얼블레이드', '소울마스터', '패스파인더', '캐논슈터', '바이퍼',
    '아크메이지(불,독)', '아델', '아델', '은월', '히어로',
    '아크', '나이트로드'
];

// 배치 15 추가
addBatch(15, BATCH_15);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('🎉 배치 15 (1401-1500위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1401-1500위 구간 분석
const batch15Jobs = BATCH_15.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch15Top = Object.entries(batch15Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1401-1500위 구간 TOP 15:');
batch15Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1500위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 20 (1-1500위)】');
distribution.slice(0, 20).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🚀 75% 완료! (1500/2000)');
console.log('💪 마지막 500개만 남았습니다! 거의 다 왔어요!');
console.log('='.repeat(80));
