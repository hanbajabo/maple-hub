import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 17 (1601-1700위) 데이터 추가
const BATCH_17 = [
    '메르세데스', '다크나이트', '은월', '팔라딘', '은월',
    '바이퍼', '비숍', '윈드브레이커', '팬텀', '윈드브레이커',
    '보우마스터', '아크메이지(불,독)', '비숍', '비숍', '에반',
    '듀얼블레이드', '듀얼블레이드', '팔라딘', '히어로', '비숍',
    '데몬어벤져', '비숍', '팔라딘', '데몬어벤져', '듀얼블레이드',
    '제로', '보우마스터', '나이트로드', '비숍', '카인',
    '아델', '팔라딘', '메르세데스', '듀얼블레이드', '카이저',
    '다크나이트', '나이트로드', '메르세데스', '팔라딘', '팔라딘',
    '은월', '듀얼블레이드', '비숍', '나이트로드', '아크메이지(불,독)',
    '팬텀', '메르세데스', '메르세데스', '아란', '나이트로드',
    '호영', '비숍', '섀도어', '메르세데스', '듀얼블레이드',
    '은월', '아델', '바이퍼', '일리움', '윈드브레이커',
    '나이트로드', '소울마스터', '듀얼블레이드', '섀도어', '비숍',
    '섀도어', '데몬어벤져', '나이트로드', '듀얼블레이드', '아크메이지(불,독)',
    '배틀메이지', '엔젤릭버스터', '나이트워커', '아델', '아크',
    '보우마스터', '제로', '비숍', '패스파인더', '나이트로드',
    '히어로', '아크메이지(불,독)', '듀얼블레이드', '아란', '섀도어',
    '에반', '제논', '아델', '듀얼블레이드', '아델',
    '윈드브레이커', '아델', '카데나', '패스파인더', '비숍',
    '아델'
];

// 배치 17 추가
addBatch(17, BATCH_17);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('🚀 배치 17 (1601-1700위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1601-1700위 구간 분석
const batch17Jobs = BATCH_17.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch17Top = Object.entries(batch17Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1601-1700위 구간 TOP 15:');
batch17Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1700위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 25 (1-1700위)】');
distribution.slice(0, 25).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎉 85% 완료! (1700/2000)');
console.log('💪 마지막 300개! 거의 끝났습니다!');
console.log('='.repeat(80));
