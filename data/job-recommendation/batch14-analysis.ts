import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 14 (1301-1400위) 데이터 추가
const BATCH_14 = [
    '에반', '나이트로드', '데몬어벤져', '엔젤릭버스터', '은월',
    '나이트워커', '듀얼블레이드', '아크메이지(썬,콜)', '다크나이트', '듀얼블레이드',
    '나이트로드', '소울마스터', '듀얼블레이드', '데몬어벤져', '윈드브레이커',
    '패스파인더', '렌', '듀얼블레이드', '아크메이지(썬,콜)', '섀도어',
    '아크메이지(썬,콜)', '아크', '히어로', '윈드브레이커', '비숍',
    '비숍', '나이트로드', '팬텀', '은월', '비숍',
    '나이트워커', '나이트워커', '엔젤릭버스터', '메카닉', '보우마스터',
    '나이트로드', '나이트로드', '나이트로드', '호영', '히어로',
    '보우마스터', '팬텀', '비숍', '엔젤릭버스터', '카인',
    '은월', '은월', '나이트로드', '아크메이지(불,독)', '듀얼블레이드',
    '호영', '제로', '듀얼블레이드', '섀도어', '듀얼블레이드',
    '아델', '다크나이트', '나이트로드', '나이트워커', '데몬슬레이어',
    '아델', '나이트로드', '팔라딘', '나이트워커', '나이트로드',
    '아크', '듀얼블레이드', '아크메이지(불,독)', '데몬슬레이어', '윈드브레이커',
    '듀얼블레이드', '아델', '아델', '바이퍼', '비숍',
    '아델', '나이트로드', '듀얼블레이드', '미하일', '호영',
    '윈드브레이커', '듀얼블레이드', '아크메이지(썬,콜)', '섀도어', '비숍',
    '캐논슈터', '캐논슈터', '비숍', '에반', '비숍',
    '와일드헌터', '데몬어벤져', '나이트로드', '듀얼블레이드', '아크메이지(썬,콜)',
    '나이트워커', '히어로', '호영', '아델'
];

// 배치 14 추가
addBatch(14, BATCH_14);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('📊 배치 14 (1301-1400위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1301-1400위 구간 분석
const batch14Jobs = BATCH_14.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch14Top = Object.entries(batch14Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1301-1400위 구간 TOP 15:');
batch14Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1400위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 20 (1-1400위)】');
distribution.slice(0, 20).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎉 70% 완료! (1400/2000)');
console.log('⏳ 남은 600개 - 마지막 스퍼트!');
console.log('='.repeat(80));
