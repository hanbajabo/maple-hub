import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 16 (1501-1600위) 데이터 추가
const BATCH_16 = [
    '비숍', '히어로', '카인', '아델', '제로',
    '듀얼블레이드', '와일드헌터', '나이트로드', '히어로', '아델',
    '팬텀', '섀도어', '나이트로드', '아델', '듀얼블레이드',
    '카이저', '윈드브레이커', '호영', '다크나이트', '에반',
    '섀도어', '블래스터', '제논', '나이트워커', '패스파인더',
    '윈드브레이커', '아델', '아델', '데몬슬레이어', '듀얼블레이드',
    '메르세데스', '윈드브레이커', '나이트로드', '렌', '캡틴',
    '데몬어벤져', '바이퍼', '아크메이지(썬,콜)', '패스파인더', '칼리',
    '아크', '메르세데스', '아델', '바이퍼', '캐논슈터',
    '패스파인더', '소울마스터', '비숍', '렌', '보우마스터',
    '아크', '나이트로드', '비숍', '아크메이지(불,독)', '카이저',
    '메르세데스', '윈드브레이커', '은월', '제논', '은월',
    '비숍', '다크나이트', '아크메이지(불,독)', '제논', '에반',
    '칼리', '나이트로드', '비숍', '라라', '제논',
    '듀얼블레이드', '데몬슬레이어', '아델', '패스파인더', '블래스터',
    '팬텀', '카데나', '메르세데스', '아크', '섀도어',
    '팔라딘', '팬텀', '히어로', '에반', '히어로',
    '윈드브레이커', '아델', '나이트로드', '섀도어', '나이트로드',
    '나이트로드', '아크메이지(불,독)', '윈드브레이커', '메르세데스', '라라',
    '아크메이지(썬,콜)', '나이트워커', '다크나이트', '제논', '엔젤릭버스터'
];

// 배치 16 추가
addBatch(16, BATCH_16);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('🚀 배치 16 (1501-1600위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1501-1600위 구간 분석
const batch16Jobs = BATCH_16.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch16Top = Object.entries(batch16Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1501-1600위 구간 TOP 15:');
batch16Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1600위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 25 (1-1600위)】');
distribution.slice(0, 25).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎉 80% 완료! (1600/2000)');
console.log('💪 마지막 400개! 거의 끝났습니다!');
console.log('='.repeat(80));
