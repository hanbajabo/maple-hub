import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 19 (1801-1900위) 데이터 추가
const BATCH_19 = [
    '팬텀', '나이트로드', '메르세데스', '데몬슬레이어', '다크나이트',
    '소울마스터', '나이트로드', '비숍', '카데나', '메르세데스',
    '아크메이지(썬,콜)', '윈드브레이커', '다크나이트', '팬텀', '팔라딘',
    '비숍', '아크메이지(불,독)', '렌', '나이트로드', '다크나이트',
    '메카닉', '나이트로드', '나이트로드', '패스파인더', '나이트로드',
    '소울마스터', '비숍', '에반', '나이트로드', '나이트로드',
    '아란', '아크메이지(불,독)', '패스파인더', '아크메이지(썬,콜)', '나이트로드',
    '데몬슬레이어', '은월', '은월', '비숍', '아크메이지(불,독)',
    '키네시스', '아델', '비숍', '배틀메이지', '나이트로드',
    '윈드브레이커', '나이트로드', '윈드브레이커', '제로', '소울마스터',
    '나이트로드', '나이트로드', '아델', '듀얼블레이드', '팬텀',
    '아크메이지(불,독)', '히어로', '배틀메이지', '비숍', '카데나',
    '에반', '제논', '메카닉', '팬텀', '나이트로드',
    '나이트로드', '엔젤릭버스터', '메르세데스', '아델', '엔젤릭버스터',
    '나이트로드', '비숍', '팔라딘', '나이트로드', '비숍',
    '메르세데스', '아크메이지(썬,콜)', '듀얼블레이드', '은월', '엔젤릭버스터',
    '히어로', '나이트워커', '비숍', '호영', '미하일',
    '비숍', '아크메이지(불,독)', '바이퍼', '아크메이지(썬,콜)', '은월',
    '아델', '팔라딘', '아크', '섀도어', '미하일',
    '메르세데스', '비숍', '비숍', '나이트로드', '팬텀'
];

// 배치 19 추가
addBatch(19, BATCH_19);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('🚀 배치 19 (1801-1900위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);

// 1801-1900위 구간 분석
const batch19Jobs = BATCH_19.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch19Top = Object.entries(batch19Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1801-1900위 구간 TOP 15:');
batch19Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 누적 분석 (1-1900위)
const distribution = analyzeDistribution();
console.log('\n【누적 TOP 30 (1-1900위)】');
distribution.slice(0, 30).forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(1)}%)`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎉 95% 완료! (1900/2000)');
console.log('💪 마지막 100개! 거의 끝났습니다!');
console.log('='.repeat(80));
