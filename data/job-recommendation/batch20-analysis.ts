import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 20 (1901-2000위) 데이터 추가 - 마지막!
const BATCH_20 = [
    '스트라이커', '아크메이지(불,독)', '나이트로드', '아크메이지(불,독)', '섀도어',
    '데몬어벤져', '아델', '아크메이지(썬,콜)', '다크나이트', '나이트로드',
    '바이퍼', '카이저', '데몬어벤져', '팔라딘', '윈드브레이커',
    '나이트워커', '비숍', '은월', '히어로', '팬텀',
    '다크나이트', '나이트워커', '나이트로드', '비숍', '듀얼블레이드',
    '에반', '듀얼블레이드', '아크메이지(불,독)', '배틀메이지', '아크',
    '렌', '아크메이지(썬,콜)', '아크메이지(썬,콜)', '듀얼블레이드', '나이트로드',
    '메르세데스', '나이트로드', '섀도어', '소울마스터', '아란',
    '팔라딘', '은월', '메르세데스', '일리움', '은월',
    '나이트로드', '아크메이지(불,독)', '소울마스터', '아델', '패스파인더',
    '윈드브레이커', '카이저', '패스파인더', '히어로', '나이트워커',
    '아란', '라라', '에반', '제논', '윈드브레이커',
    '캡틴', '비숍', '제로', '제로', '팔라딘',
    '나이트로드', '제로', '패스파인더', '나이트로드', '패스파인더',
    '메카닉', '제로', '엔젤릭버스터', '윈드브레이커', '제논',
    '에반', '듀얼블레이드', '듀얼블레이드', '다크나이트', '아크메이지(불,독)',
    '나이트로드', '나이트로드', '아크메이지(썬,콜)', '제로', '비숍',
    '메르세데스', '바이퍼', '비숍', '메카닉', '히어로',
    '카인', '배틀메이지', '아크메이지(불,독)', '렌', '나이트로드',
    '카데나', '아델', '아델', '나이트워커', '나이트로드'
];

// 배치 20 추가
addBatch(20, BATCH_20);

// 🎊🎊🎊 완료! 🎊🎊🎊
console.log('\n');
console.log('🎉'.repeat(40));
console.log('🏆🏆🏆 배치 20 (1901-2000위) 추가 완료! - 전체 데이터 수집 완료! 🏆🏆🏆');
console.log('🎉'.repeat(40));

const status = getCollectionStatus();
console.log(`\n✅ 최종 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20 - 전체 완료!`);

// 1901-2000위 구간 분석
const batch20Jobs = BATCH_20.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch20Top = Object.entries(batch20Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1901-2000위 구간 TOP 15:');
batch20Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 최종 전체 분석 (1-2000위)
const distribution = analyzeDistribution();
console.log('\n' + '='.repeat(80));
console.log('【최종 전체 47개 직업 분포 (1-2000위)】');
console.log('='.repeat(80));
distribution.forEach((d, i) => {
    console.log(
        `${(i + 1).toString().padStart(2)}. ${d.job.padEnd(20)} ` +
        `${d.count.toString().padStart(3)}개 (${d.percentage.toFixed(2)}%) ` +
        `평균 ${d.averageRank.toFixed(0)}위 ` +
        `[${d.bestRank}위~${d.worstRank}위]`
    );
});

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎊🎊🎊 축하합니다! TOP 2000 직업 분포 분석 완료! 🎊🎊🎊');
console.log('='.repeat(80));
