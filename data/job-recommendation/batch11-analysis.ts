import { addBatch, printResults, analyzeDistribution, getCollectionStatus } from './top2000-collector';

// 배치 11 (1001-1100위) 데이터 추가
const BATCH_11 = [
    '듀얼블레이드', '미하일', '소울마스터', '데몬슬레이어', '윈드브레이커',
    '나이트로드', '팬텀', '다크나이트', '미하일', '팔라딘',
    '나이트워커', '팔라딘', '나이트로드', '보우마스터', '바이퍼',
    '메르세데스', '엔젤릭버스터', '아크메이지(불,독)', '메르세데스', '히어로',
    '카데나', '제논', '스트라이커', '아델', '소울마스터',
    '나이트로드', '소울마스터', '듀얼블레이드', '카데나', '팬텀',
    '아크메이지(썬,콜)', '윈드브레이커', '캐논슈터', '제논', '듀얼블레이드',
    '아크메이지(썬,콜)', '듀얼블레이드', '듀얼블레이드', '메르세데스', '나이트로드',
    '팔라딘', '은월', '칼리', '칼리', '데몬슬레이어',
    '메카닉', '비숍', '데몬슬레이어', '루미너스', '제로',
    '제논', '캐논슈터', '아크', '아크메이지(썬,콜)', '나이트워커',
    '아크메이지(불,독)', '아크메이지(썬,콜)', '아크', '나이트로드', '패스파인더',
    '비숍', '은월', '나이트로드', '아크메이지(불,독)', '아크메이지(불,독)',
    '아크메이지(불,독)', '카데나', '비숍', '메르세데스', '아크',
    '카이저', '아델', '히어로', '에반', '바이퍼',
    '패스파인더', '제논', '메카닉', '신궁', '렌',
    '나이트로드', '아크메이지(불,독)', '스트라이커', '나이트워커', '보우마스터',
    '나이트로드', '비숍', '보우마스터', '제논', '나이트워커',
    '히어로', '보우마스터', '윈드브레이커', '아델', '아크메이지(불,독)',
    '메르세데스', '아크메이지(불,독)', '비숍', '미하일', '데몬슬레이어'
];

// 배치 11 추가 (실제로는 기존 1-10 배치도 먼저 추가되어야 함)
addBatch(11, BATCH_11);

// 현재까지 결과 출력
console.log('\n');
console.log('='.repeat(80));
console.log('📊 배치 11 (1001-1100위) 추가 완료!');
console.log('='.repeat(80));

const status = getCollectionStatus();
console.log(`\n✅ 현재 진행률: ${status.progress.toFixed(1)}%`);
console.log(`📦 수집된 배치: ${status.batchesCollected}/20`);
console.log(`⏳ 남은 배치: ${status.remainingBatches}개`);

// 1001-1100위 구간 분석
const batch11Jobs = BATCH_11.reduce((acc, job) => {
    acc[job] = (acc[job] || 0) + 1;
    return acc;
}, {} as Record<string, number>);

const batch11Top = Object.entries(batch11Jobs)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 15);

console.log('\n🔥 1001-1100위 구간 TOP 15:');
batch11Top.forEach(([job, count], i) => {
    console.log(`  ${(i + 1).toString().padStart(2)}. ${job.padEnd(20)} ${count}개`);
});

// 특징 분석
const firePoison = BATCH_11.filter(j => j === '아크메이지(불,독)').length;
const nightlord = BATCH_11.filter(j => j === '나이트로드').length;
const dualBlade = BATCH_11.filter(j => j === '듀얼블레이드').length;
const mihile = BATCH_11.filter(j => j === '미하일').length;

console.log(`\n📊 주요 직업 분석 (1001-1100위):`);
console.log(`  - 나이트로드: ${nightlord}개`);
console.log(`  - 듀얼블레이드: ${dualBlade}개`);
console.log(`  - 아크메이지(불,독): ${firePoison}개`);
console.log(`  - 미하일: ${mihile}개 (희귀 직업 등장 증가!)`);

printResults();

console.log('\n' + '='.repeat(80));
console.log('🎯 배치 11 완료! 다음 배치 (1101-1200위) 데이터를 기다립니다!');
console.log('='.repeat(80));
